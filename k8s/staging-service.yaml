apiVersion: v1
kind: Service
metadata:
  name: product-api-staging-service
  namespace: staging
spec:
  selector:
    app: product-api-staging
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
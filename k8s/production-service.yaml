apiVersion: v1
kind: Service
metadata:
  name: product-api-production-service
  namespace: production
spec:
  selector:
    app: product-api-production
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP